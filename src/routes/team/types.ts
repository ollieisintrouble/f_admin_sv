export type Member = {
    username: string,
    fullName: string | null,
    email: string | null,
    phone: string | null,
    image: string | null,
    createdAt: Date | null;
    role: string | null,
}
